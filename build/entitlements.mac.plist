<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<!--
  Entitlements do IrisFlow para o Hardened Runtime do macOS.

  Só entram em vigor quando o app é ASSINADO com Developer ID (CSC_LINK) —
  electron/package-app.mjs liga `hardenedRuntime` nesse caso. Sem certificado
  o app sai com assinatura ad-hoc e sem Hardened Runtime.

  - allow-jit / allow-unsigned-executable-memory: o V8 do Electron e o
    WebAssembly (MediaPipe, ONNX Runtime) compilam código em tempo de execução.
  - disable-library-validation: o motor de voz e módulos nativos carregados
    por caminho não são assinados pela mesma equipe que o app.
  - device.camera: rastreamento do olhar pela webcam (NSCameraUsageDescription).
  - device.audio-input: gravar a voz de referência da voz personalizada, a
    pedido do cuidador (NSMicrophoneUsageDescription).
  - network.client: login e mensagens do cuidador (Supabase), atualização.
-->
<plist version="1.0">
  <dict>
    <key>com.apple.security.cs.allow-jit</key>
    <true/>
    <key>com.apple.security.cs.allow-unsigned-executable-memory</key>
    <true/>
    <key>com.apple.security.cs.disable-library-validation</key>
    <true/>
    <key>com.apple.security.device.camera</key>
    <true/>
    <key>com.apple.security.device.audio-input</key>
    <true/>
    <key>com.apple.security.network.client</key>
    <true/>
  </dict>
</plist>
