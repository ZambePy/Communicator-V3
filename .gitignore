# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
dist-electron
release
*.local

# Modelos treinados (binários grandes, não versionados)
resources/models/*.onnx
frontend/public/models/**/*.onnx

# Datasets e artefatos ML pesados
python_ml/datasets/
python_ml/.git/

# Scratch / experimentos externos
scratch/

# Relatórios do teste de precisão gravados na raiz pelo app. Para guardar um,
# mova para docs/medicoes/historico/ (ver docs/MEDICOES.md).
/accuracy-report-*.json

# Fotos de referência da validação de eixos do L2CS (biometria, não versionar).
frontend/python_scripts/l2cs_validation/

# Gravações de tela (referência visual/QA — pesadas, não versionadas)
*.mp4
*.mov
*.mkv
*.webm

# Claude Code
.claude/

# Saídas geradas pelo Claude (rascunhos de configuração, roteiros, YAMLs de
# apoio). Não é código do produto — fica fora do repositório.
Claude outputs/

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntv's*
*.njsproj
*.sln
*.sw?

# Motor de voz local (Python): ambiente virtual, build do PyInstaller e caches.
# O áudio de referência e os pesos do modelo ficam na pasta de dados do app,
# nunca no repositório.
voice-engine/.venv/
voice-engine/build/
voice-engine/dist/
voice-engine/*.spec
voice-engine/**/__pycache__/
voice-engine/.pytest_cache/

# ─────────────────────────────────────────────────────────────────────────────
# Documentação, anotações e arquivos de dados fora do código
#
# Decisão de 10/09/2026, apertada em 23/09/2026: o repositório guarda CÓDIGO.
# Texto solto (.txt), documentação (.md) e planilhas ficam de fora, porque
# quase todos carregam alguma coisa que não deve ir para um repositório
# público — credenciais, roteiros internos, e principalmente qualquer coisa
# com dado de paciente. Ficam só dois documentos: o README.md da raiz (que
# descreve o monorepo inteiro — desktop, site, app, banco e hospedagem) e o
# protocolo de medição, que é a base metodológica do projeto. Os .gitignore
# de site/ e app/ seguem a mesma regra, sem exceções.
# ─────────────────────────────────────────────────────────────────────────────
*.txt
*.md
!/README.md
!/docs/MEDICOES.md

# Duas exceções técnicas ao "*.txt" acima. Se quiser mesmo tirar as duas do
# repositório, apague estas linhas — mas saiba o que cada uma quebra:
#   - requirements.txt é a lista de dependências do motor de voz; sem ele,
#     ninguém consegue reconstruir o motor a partir do repositório.
#   - a licença OFL precisa acompanhar o arquivo da fonte. Versionar a fonte
#     sem a licença é distribuição irregular.
!voice-engine/requirements.txt
!**/*-OFL.txt

# Planilhas e exportações tabulares (o modelo do protocolo semanal é o único
# arquivo desse tipo que deveria existir aqui, e ele vive fora do repositório
# junto com os dados reais das casas).
*.csv
*.tsv
*.xlsx

# Documentos de escritório: plano de negócios, apresentações, contratos e PDFs
# em geral. Mesma decisão do "*.md"/"*.txt": documentação de projeto fica
# fora do repositório de código.
*.doc
*.docx
*.pdf
*.ppt
*.pptx
*.odt
*.rtf

# Arquivos que o próprio aplicativo gera na máquina de quem usa. Nenhum deles
# é código, e todos podem conter dado de sessão real.
irisflow-suporte-*.json
irisflow-recording-*.jsonl
irisflow_backup.json
relatorio-clinico-paciente-*.json

# Credenciais. O .env já não entrava pelo frontend/.gitignore, mas a raiz não
# tinha regra nenhuma — e é onde as chaves acabam parando.
.env
.env.*
!.env.example
*.pem
*.key
*.pfx
*.p12
*.cer
*.crt

# Áudio: a gravação de referência da voz clonada é biometria do paciente.
*.wav
*.opus
*.mp3
*.m4a
*.flac
*.ogg

# Pesos de modelo e binários grandes (o *.onnx já estava coberto por caminho;
# aqui vale para qualquer pasta).
*.onnx
*.pth
*.pt
*.safetensors
*.ckpt

# Instaladores e pacotes gerados pelo empacotamento.
*.exe
*.msi
*.dmg
*.AppImage
*.zip
*.7z
*.rar

# Cobertura de testes e caches de ferramenta.
coverage/
.nyc_output/
.vite/
*.tsbuildinfo

# Lixo de sistema operacional e de editor (Windows cria desktop.ini e
# Thumbs.db em pastas sincronizadas pelo OneDrive; o Office deixa ~$arquivo
# enquanto um documento está aberto).
desktop.ini
Thumbs.db
ehthumbs.db
$RECYCLE.BIN/
~$*
*.tmp

# Caches de Python fora do motor de voz e artefatos do Playwright.
__pycache__/
*.pyc
playwright-report/
test-results/
